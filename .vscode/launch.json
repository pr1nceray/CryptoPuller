{
    "configurations": [
    {
        "name": "(lldb) Launch",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/trader_debug",
        "cwd": "${workspaceFolder}/",
        "stdio": [
        ]
    
    }
    ]
}