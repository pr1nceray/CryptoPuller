{
    "configurations": [
    {
        "name": "(lldb) Launch",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/crypto_debug",
        "cwd": "${workspaceFolder}/",
        "stdio": [
        ]
    
    }
    ]
}